// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Wei-Shaw/sub2api/ent/paymentproviderfeeevent"
	"github.com/Wei-Shaw/sub2api/ent/predicate"
)

// PaymentProviderFeeEventQuery is the builder for querying PaymentProviderFeeEvent entities.
type PaymentProviderFeeEventQuery struct {
	config
	ctx        *QueryContext
	order      []paymentproviderfeeevent.OrderOption
	inters     []Interceptor
	predicates []predicate.PaymentProviderFeeEvent
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PaymentProviderFeeEventQuery builder.
func (_q *PaymentProviderFeeEventQuery) Where(ps ...predicate.PaymentProviderFeeEvent) *PaymentProviderFeeEventQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *PaymentProviderFeeEventQuery) Limit(limit int) *PaymentProviderFeeEventQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *PaymentProviderFeeEventQuery) Offset(offset int) *PaymentProviderFeeEventQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *PaymentProviderFeeEventQuery) Unique(unique bool) *PaymentProviderFeeEventQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *PaymentProviderFeeEventQuery) Order(o ...paymentproviderfeeevent.OrderOption) *PaymentProviderFeeEventQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// First returns the first PaymentProviderFeeEvent entity from the query.
// Returns a *NotFoundError when no PaymentProviderFeeEvent was found.
func (_q *PaymentProviderFeeEventQuery) First(ctx context.Context) (*PaymentProviderFeeEvent, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{paymentproviderfeeevent.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *PaymentProviderFeeEventQuery) FirstX(ctx context.Context) *PaymentProviderFeeEvent {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PaymentProviderFeeEvent ID from the query.
// Returns a *NotFoundError when no PaymentProviderFeeEvent ID was found.
func (_q *PaymentProviderFeeEventQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{paymentproviderfeeevent.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *PaymentProviderFeeEventQuery) FirstIDX(ctx context.Context) int64 {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PaymentProviderFeeEvent entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PaymentProviderFeeEvent entity is found.
// Returns a *NotFoundError when no PaymentProviderFeeEvent entities are found.
func (_q *PaymentProviderFeeEventQuery) Only(ctx context.Context) (*PaymentProviderFeeEvent, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{paymentproviderfeeevent.Label}
	default:
		return nil, &NotSingularError{paymentproviderfeeevent.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *PaymentProviderFeeEventQuery) OnlyX(ctx context.Context) *PaymentProviderFeeEvent {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PaymentProviderFeeEvent ID in the query.
// Returns a *NotSingularError when more than one PaymentProviderFeeEvent ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *PaymentProviderFeeEventQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{paymentproviderfeeevent.Label}
	default:
		err = &NotSingularError{paymentproviderfeeevent.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *PaymentProviderFeeEventQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PaymentProviderFeeEvents.
func (_q *PaymentProviderFeeEventQuery) All(ctx context.Context) ([]*PaymentProviderFeeEvent, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PaymentProviderFeeEvent, *PaymentProviderFeeEventQuery]()
	return withInterceptors[[]*PaymentProviderFeeEvent](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *PaymentProviderFeeEventQuery) AllX(ctx context.Context) []*PaymentProviderFeeEvent {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PaymentProviderFeeEvent IDs.
func (_q *PaymentProviderFeeEventQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(paymentproviderfeeevent.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *PaymentProviderFeeEventQuery) IDsX(ctx context.Context) []int64 {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *PaymentProviderFeeEventQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*PaymentProviderFeeEventQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *PaymentProviderFeeEventQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *PaymentProviderFeeEventQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *PaymentProviderFeeEventQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PaymentProviderFeeEventQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *PaymentProviderFeeEventQuery) Clone() *PaymentProviderFeeEventQuery {
	if _q == nil {
		return nil
	}
	return &PaymentProviderFeeEventQuery{
		config:     _q.config,
		ctx:        _q.ctx.Clone(),
		order:      append([]paymentproviderfeeevent.OrderOption{}, _q.order...),
		inters:     append([]Interceptor{}, _q.inters...),
		predicates: append([]predicate.PaymentProviderFeeEvent{}, _q.predicates...),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		PaymentOrderID int64 `json:"payment_order_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PaymentProviderFeeEvent.Query().
//		GroupBy(paymentproviderfeeevent.FieldPaymentOrderID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *PaymentProviderFeeEventQuery) GroupBy(field string, fields ...string) *PaymentProviderFeeEventGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PaymentProviderFeeEventGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = paymentproviderfeeevent.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		PaymentOrderID int64 `json:"payment_order_id,omitempty"`
//	}
//
//	client.PaymentProviderFeeEvent.Query().
//		Select(paymentproviderfeeevent.FieldPaymentOrderID).
//		Scan(ctx, &v)
func (_q *PaymentProviderFeeEventQuery) Select(fields ...string) *PaymentProviderFeeEventSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &PaymentProviderFeeEventSelect{PaymentProviderFeeEventQuery: _q}
	sbuild.label = paymentproviderfeeevent.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PaymentProviderFeeEventSelect configured with the given aggregations.
func (_q *PaymentProviderFeeEventQuery) Aggregate(fns ...AggregateFunc) *PaymentProviderFeeEventSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *PaymentProviderFeeEventQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !paymentproviderfeeevent.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *PaymentProviderFeeEventQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PaymentProviderFeeEvent, error) {
	var (
		nodes = []*PaymentProviderFeeEvent{}
		_spec = _q.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PaymentProviderFeeEvent).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PaymentProviderFeeEvent{config: _q.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (_q *PaymentProviderFeeEventQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *PaymentProviderFeeEventQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(paymentproviderfeeevent.Table, paymentproviderfeeevent.Columns, sqlgraph.NewFieldSpec(paymentproviderfeeevent.FieldID, field.TypeInt64))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, paymentproviderfeeevent.FieldID)
		for i := range fields {
			if fields[i] != paymentproviderfeeevent.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *PaymentProviderFeeEventQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(paymentproviderfeeevent.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = paymentproviderfeeevent.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *PaymentProviderFeeEventQuery) ForUpdate(opts ...sql.LockOption) *PaymentProviderFeeEventQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *PaymentProviderFeeEventQuery) ForShare(opts ...sql.LockOption) *PaymentProviderFeeEventQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// PaymentProviderFeeEventGroupBy is the group-by builder for PaymentProviderFeeEvent entities.
type PaymentProviderFeeEventGroupBy struct {
	selector
	build *PaymentProviderFeeEventQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *PaymentProviderFeeEventGroupBy) Aggregate(fns ...AggregateFunc) *PaymentProviderFeeEventGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *PaymentProviderFeeEventGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PaymentProviderFeeEventQuery, *PaymentProviderFeeEventGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *PaymentProviderFeeEventGroupBy) sqlScan(ctx context.Context, root *PaymentProviderFeeEventQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PaymentProviderFeeEventSelect is the builder for selecting fields of PaymentProviderFeeEvent entities.
type PaymentProviderFeeEventSelect struct {
	*PaymentProviderFeeEventQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *PaymentProviderFeeEventSelect) Aggregate(fns ...AggregateFunc) *PaymentProviderFeeEventSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *PaymentProviderFeeEventSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PaymentProviderFeeEventQuery, *PaymentProviderFeeEventSelect](ctx, _s.PaymentProviderFeeEventQuery, _s, _s.inters, v)
}

func (_s *PaymentProviderFeeEventSelect) sqlScan(ctx context.Context, root *PaymentProviderFeeEventQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
